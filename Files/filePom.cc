// localTable: tester, 2, 0
double tester(string lol, double testik, int next);

// localTable: main, 2, 0 
int main(int param, string param2)
{
    // stack->top: localTable
    
    // nova tabulka symbolu, temp
    // temp: test, 1, 0
    int test = 0;
    cin >> test; // kontrola, jestli test byl definovan
  

    int test;
    // tohle resi dusan v expr
    if(test + (lol + neco))
    {
        // nova tabulka symbolu, temp
        // temp: ifova, 1, 0
        int ifova = 2;
        for(int param = 0; param + param2; param = param)
        {
            cout << test;
        }
    }
 
    return test;
}
 
// localTable: tester, 2, 1
double tester(string lol, double testik, int next)
{
    if(testik)
    {
        return testik + (neco + (neco + lol));
    }
       
    return 0;
}

// localTable: lolicek, 2, 0
string wefkewpfpeowfkoewfkpowk()
{
	
}